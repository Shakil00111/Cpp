#include<iostream>
using namespace std;

int main() {

    // Ternary operator example
    int a = 20, b = 30;
    int max = (a > b) ? a : b;
    cout << "The maximum value is: " << max << endl;

    return 0;
}