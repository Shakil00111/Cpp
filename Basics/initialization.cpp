#include<iostream>
using namespace std;

int main() {

    // Initialize a variable with a value

    // Initialize with brackets method
    int x{10};

    // Initialize with assignment operator
    int y = 20;

    // Initialize with function method
    int z(30);

    cout << x << endl << y << endl << z << endl;

    return 0;
}