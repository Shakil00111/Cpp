#include<iostream>
using namespace std;

void isPrime(int num);

int main() {

    int  num;

    cout<<"Enter a number : ";
    cin>>num;

    isPrime(num);

    return 0;
}

void isPrime(int num) {

    if(num <= 0) {
        cout<<"You must input a number greater than 0."<<endl;
    } else {
        if
    }
}